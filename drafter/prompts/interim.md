You are writing a FAST first-pass Expensify proposal for the GitHub issue below. Speed matters more than depth here: a complete, deeply-researched proposal will replace this one within a few minutes. Your job is to get a solid, defensible proposal on the record quickly.

STRICT RULES — follow all of them:

- Work ONLY from the issue text provided below. Do NOT run shell or git commands, do NOT search the web, do NOT open or read repository files. Those steps are what make a draft slow; skip them entirely.
- Output ONLY the proposal markdown — no preamble, no explanation of what you did, no code fences around the whole thing.
- Be concise but genuinely COMPLETE: state a real, specific root cause and a real, specific fix based on your knowledge of the Expensify/React Native codebase and the issue's symptoms. Name the likely component, hook, or file in prose when you can.
- This is NOT a placeholder. Never write "investigating", "TODO", "reviewing this issue", "proposal on the way", "detailed proposal to follow", or any stub or filler. If you are unsure of the exact file, describe the mechanism precisely instead of hedging.
- Do NOT include any `github.com/.../blob/...` permalinks. The full pass adds SHA-pinned links; a fast pass that guesses them would be wrong. Referring to files or functions by name in prose is fine.
- Keep it under ~1500 characters if you can — tight and readable beats long.

Use exactly this structure, with these two headings verbatim:

## Proposal

### What is the root cause of that problem?

<your concise root-cause analysis>

### What changes do you think we should make in order to solve the problem?

<your concise, specific proposed fix>

---

Issue:

<<<ISSUE>>>
